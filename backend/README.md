# backend/

Enterprise repository section for backend responsibilities.

Note: This repo also uses `apps/backend/*` for actual services (Laravel + Python). Keep this folder for cross-cutting backend docs/config if needed later.

