"""
Legacy toy-data generator retained for reference only.

IMPORTANT: The production training workflow uses data/phishing_urls.csv
(the supplied real-world phishing dataset) and data/training_urls.csv.
Do not run this script unless you intentionally want a tiny demonstration set.
"""
print("Production training uses data/phishing_urls.csv and data/training_urls.csv.")
print("This legacy generator is disabled so it cannot overwrite the real dataset.")
